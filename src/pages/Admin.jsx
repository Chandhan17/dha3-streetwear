import { useCallback, useEffect, useMemo, useState } from 'react'
import { useLocation, useNavigate } from 'react-router-dom'
import { AnimatePresence, motion as Motion } from 'framer-motion'
import clientConfig from '../config'
import AdminLayout from '../components/AdminLayout'
import Badge from '../components/Badge'
import Button from '../components/Button'
import EmptyState from '../components/EmptyState'
import Input from '../components/Input'
import Loader from '../components/Loader'
import Modal from '../components/Modal'
import StatCard from '../components/StatCard'
import Table from '../components/Table'
import { deleteUserProfile, getCurrentAdmin, logoutAdmin, updateAdminPasswordSecure, updateAdminProfile } from '../services/authService'
import { deleteOrder, getOrders, updateOrderStatus } from '../services/orderService'
import { uploadImage } from '../services/uploadService'
import { createProduct, editProduct, fetchProducts, removeProduct } from '../services/productService'

const fallbackCategories = ['Shirts', 'T-Shirts', 'Jeans', 'Shoes', 'Accessories']
const adminCategories = Array.isArray(clientConfig.productCategories) && clientConfig.productCategories.length ? clientConfig.productCategories : fallbackCategories
const pageSize = 8

const getInitialFormState = () => ({
  name: '', price: '', category: adminCategories[0], sizesInput: '', description: '', sizeChartText: '', sizeChartImage: '', sizeChartImageFile: null,
  sku: '', barcode: '', purchasePrice: '', mrp: '', discountPercent: '', gstPercent: '0', hsnCode: '', openingStock: '0', openingStockValue: '',
})

function parseSizesInput(sizesInput) { return [...new Set(String(sizesInput || '').split(',').map((size) => size.trim()).filter(Boolean))] }
function getProductImages(product) { if (Array.isArray(product.images) && product.images.length > 0) return product.images; const fallbackImage = product.imageUrl || product.image || ''; return fallbackImage ? [fallbackImage] : [] }
function getFileSignature(file) { return `${file.name}-${file.size}-${file.lastModified}` }
function mergeImageFiles(currentFiles, incomingFiles) { const nextFiles = [...currentFiles]; const existingSignatures = new Set(currentFiles.map(getFileSignature)); incomingFiles.forEach((file) => { const signature = getFileSignature(file); if (!existingSignatures.has(signature)) { existingSignatures.add(signature); nextFiles.push(file) } }); return nextFiles }
function formatCurrency(value) { return `Rs. ${new Intl.NumberFormat('en-IN').format(Number(value || 0))}` }
function formatDateTime(value) { if (!value) return '-'; const date = value instanceof Date ? value : new Date(value); if (Number.isNaN(date.getTime())) return '-'; return date.toLocaleString('en-IN', { dateStyle: 'medium', timeStyle: 'short' }) }
function getTimestampMs(value) { if (!value) return null; if (value instanceof Date) return value.getTime(); if (typeof value?.toDate === 'function') return value.toDate().getTime(); const seconds = Number(value?.seconds ?? value?._seconds); const nanoseconds = Number(value?.nanoseconds ?? value?._nanoseconds ?? 0); if (Number.isFinite(seconds)) return seconds * 1000 + Math.floor(nanoseconds / 1000000); const parsedDate = new Date(value); return Number.isNaN(parsedDate.getTime()) ? null : parsedDate.getTime() }
function getOrderStatusTone(status) { if (status === 'completed' || status === 'delivered') return 'success'; if (status === 'processing' || status === 'shipped') return 'info'; if (status === 'cancelled') return 'danger'; return 'warning' }

function Admin() {
  const location = useLocation()
  const navigate = useNavigate()
  const [activeSection, setActiveSection] = useState(() => location.pathname === '/admin/products' ? 'products' : 'dashboard')
  const [query, setQuery] = useState('')
  const [toast, setToast] = useState({ type: '', message: '' })
  const [products, setProducts] = useState([]); const [orders, setOrders] = useState([]); const [isLoadingProducts, setIsLoadingProducts] = useState(true); const [isLoadingOrders, setIsLoadingOrders] = useState(true)
  const [isSaving, setIsSaving] = useState(false); const [isUploadingImages, setIsUploadingImages] = useState(false); const [isLoggingOut, setIsLoggingOut] = useState(false)
  const [viewMode, setViewMode] = useState('grid'); const [productCategoryFilter, setProductCategoryFilter] = useState('all'); const [currentPage, setCurrentPage] = useState(1); const [selectedProductIds, setSelectedProductIds] = useState([]); const [activeCardImageIndex, setActiveCardImageIndex] = useState({})
  const [showProductModal, setShowProductModal] = useState(false); const [showDeleteModal, setShowDeleteModal] = useState(false); const [deletingProductId, setDeletingProductId] = useState(''); const [editingProduct, setEditingProduct] = useState(null)
  const [images, setImages] = useState([]); const [retainedExistingImages, setRetainedExistingImages] = useState([]); const [fileInputKey, setFileInputKey] = useState(Date.now()); const [formValues, setFormValues] = useState(getInitialFormState())
  const [orderStatusFilter, setOrderStatusFilter] = useState('all'); const [orderSort, setOrderSort] = useState('newest'); const [selectedOrderId, setSelectedOrderId] = useState(null); const [deletingOrderId, setDeletingOrderId] = useState(''); const [selectedOrderIds, setSelectedOrderIds] = useState([]); const [bulkOrderStatus, setBulkOrderStatus] = useState(''); const [isBulkUpdatingOrders, setIsBulkUpdatingOrders] = useState(false); const [isBulkDeletingOrders, setIsBulkDeletingOrders] = useState(false); const [deletingUserId, setDeletingUserId] = useState('')
  const [profileForm, setProfileForm] = useState({ displayName: '', email: '', currentPassword: '' }); const [passwordForm, setPasswordForm] = useState({ currentPassword: '', newPassword: '' }); const [isSavingProfile, setIsSavingProfile] = useState(false); const [isSavingPassword, setIsSavingPassword] = useState(false)
  const selectedImagePreviews = useMemo(() => images.map((file) => URL.createObjectURL(file)), [images])
  useEffect(() => () => selectedImagePreviews.forEach((previewUrl) => URL.revokeObjectURL(previewUrl)), [selectedImagePreviews])
  const pushToast = (type, message) => setToast({ type, message })
  useEffect(() => { if (!toast.message) return; const timer = window.setTimeout(() => setToast({ type: '', message: '' }), 2600); return () => window.clearTimeout(timer) }, [toast])
  const loadProducts = useCallback(async () => { setIsLoadingProducts(true); try { setProducts(await fetchProducts({ forceRefresh: true })) } catch (error) { pushToast('error', error.message || 'Unable to fetch products.') } finally { setIsLoadingProducts(false) } }, [])
  const loadOrders = useCallback(async () => { setIsLoadingOrders(true); try { setOrders(await getOrders()) } catch (error) { pushToast('error', error.message || 'Unable to fetch orders.') } finally { setIsLoadingOrders(false) } }, [])
  useEffect(() => { loadProducts(); loadOrders() }, [loadOrders, loadProducts])
  useEffect(() => { const adminUser = getCurrentAdmin(); setProfileForm((current) => ({ ...current, displayName: adminUser?.displayName || 'Admin', email: adminUser?.email || '' })) }, [])
  const resetForm = () => { setFormValues(getInitialFormState()); setImages([]); setRetainedExistingImages([]); setEditingProduct(null); setFileInputKey(Date.now()) }
  const validateForm = () => { if (!formValues.name.trim() || !formValues.price) { pushToast('error', 'Product name and price are required.'); return false } if (Number(formValues.price) < 0 || Number(formValues.purchasePrice || 0) < 0 || Number(formValues.mrp || 0) < 0 || Number(formValues.openingStock || 0) < 0 || Number(formValues.discountPercent || 0) < 0 || Number(formValues.discountPercent || 0) > 100) { pushToast('error', 'Prices and opening stock cannot be negative.'); return false } if (Number(formValues.discountPercent || 0) < 0 || Number(formValues.discountPercent || 0) > 100) { pushToast('error', 'Discount must be between 0 and 100%.'); return false } if (Number(formValues.gstPercent || 0) < 0 || Number(formValues.gstPercent || 0) > 100) { pushToast('error', 'GST must be between 0 and 100%.'); return false } if (!editingProduct && images.length === 0) { pushToast('error', 'Please upload at least one product image.'); return false } if (editingProduct && images.length === 0 && retainedExistingImages.length === 0) { pushToast('error', 'Please keep at least one image before saving.'); return false } return true }
  const handleInputChange = (event) => { const { name, value } = event.target; setFormValues((previousValues) => ({ ...previousValues, [name]: value })) }
  const handleImageChange = (event) => { const selectedFiles = Array.from(event.target.files || []); if (!selectedFiles.length) return; setImages((currentFiles) => mergeImageFiles(currentFiles, selectedFiles)); event.target.value = '' }
  const handleSizeChartImageChange = (event) => setFormValues((previousValues) => ({ ...previousValues, sizeChartImageFile: event.target.files?.[0] || null }))
  const handleRemoveSelectedImage = (fileToRemove) => { const signatureToRemove = getFileSignature(fileToRemove); setImages((currentFiles) => currentFiles.filter((file) => getFileSignature(file) !== signatureToRemove)) }
  const handleRemoveExistingImage = (indexToRemove) => setRetainedExistingImages((currentImages) => currentImages.filter((_, index) => index !== indexToRemove))
  const handleOpenCreate = () => { resetForm(); setShowProductModal(true) }
  const handleOpenEdit = (product) => { setEditingProduct(product); setFormValues({ name: product.name, price: String(product.price), category: product.category || adminCategories[0], sizesInput: Array.isArray(product.sizes) ? product.sizes.join(',') : '', description: product.description || '', sizeChartText: product.sizeChartText || '', sizeChartImage: product.sizeChartImage || '', sizeChartImageFile: null, sku: product.sku || '', barcode: product.barcode || '', purchasePrice: String(product.purchasePrice ?? ''), mrp: String(product.mrp ?? product.price ?? ''), discountPercent: String(product.discountPercent ?? ''), gstPercent: String(product.gstPercent ?? 0), hsnCode: product.hsnCode || '', openingStock: String(product.openingStock ?? product.stock ?? 0), openingStockValue: String(product.openingStockValue ?? '') }); setRetainedExistingImages(getProductImages(product)); setImages([]); setShowProductModal(true); setFileInputKey(Date.now()) }
  const handleSubmitProduct = async (event) => { event.preventDefault(); if (!validateForm()) return; setIsSaving(true); setIsUploadingImages(images.length > 0 || Boolean(formValues.sizeChartImageFile)); try { const common = { name: formValues.name, price: formValues.price, category: formValues.category, sizes: parseSizesInput(formValues.sizesInput), description: formValues.description, sizeChartImage: formValues.sizeChartImageFile ? await uploadImage(formValues.sizeChartImageFile) : formValues.sizeChartImage, sizeChartText: formValues.sizeChartText, sku: formValues.sku, barcode: formValues.barcode, purchasePrice: formValues.purchasePrice, mrp: formValues.mrp, discountPercent: formValues.discountPercent, gstPercent: formValues.gstPercent, hsnCode: formValues.hsnCode, openingStock: formValues.openingStock, openingStockValue: formValues.openingStockValue }; if (editingProduct) { await editProduct(editingProduct.id, { ...common, imageFiles: images, currentImages: retainedExistingImages, currentImageUrl: editingProduct.imageUrl || editingProduct.image, stock: editingProduct.stock }); pushToast('success', 'Product updated successfully.') } else { await createProduct({ ...common, imageFiles: images }); pushToast('success', 'Product added successfully.') } setShowProductModal(false); resetForm(); await loadProducts() } catch (error) { pushToast('error', error.message || 'Failed to save product.') } finally { setIsSaving(false); setIsUploadingImages(false) } }
  const handleDelete = async (productId) => { setDeletingProductId(productId); try { await removeProduct(productId); setProducts((current) => current.filter((product) => product.id !== productId)); setSelectedProductIds((current) => current.filter((id) => id !== productId)); pushToast('success', 'Product deleted successfully.') } catch (error) { pushToast('error', error.message || 'Failed to delete product.') } finally { setShowDeleteModal(false); setDeletingProductId('') } }
  const handleBulkDeleteProducts = async () => { if (!selectedProductIds.length) return; try { await Promise.all(selectedProductIds.map((id) => removeProduct(id))); setProducts((current) => current.filter((product) => !selectedProductIds.includes(product.id))); setSelectedProductIds([]); pushToast('success', 'Selected products deleted.') } catch { pushToast('error', 'Failed to bulk delete products.') } }
  const handleCopyProductLink = async (productId) => { try { await navigator.clipboard.writeText(`${window.location.origin}/product/${productId}`); pushToast('success', 'Product link copied.') } catch { pushToast('error', 'Unable to copy product link.') } }
  const handleLogout = async () => { setIsLoggingOut(true); try { await logoutAdmin(); navigate('/login', { replace: true }) } catch (error) { pushToast('error', error.message || 'Failed to logout.') } finally { setIsLoggingOut(false) } }
  const handleSaveProfile = async () => { setIsSavingProfile(true); try { await updateAdminProfile(profileForm); const adminUser = getCurrentAdmin(); setProfileForm((current) => ({ ...current, displayName: adminUser?.displayName || current.displayName, email: adminUser?.email || current.email, currentPassword: '' })); pushToast('success', 'Profile updated successfully.') } catch (error) { pushToast('error', error.message || 'Failed to update profile.') } finally { setIsSavingProfile(false) } }
  const handleSavePassword = async () => { setIsSavingPassword(true); try { await updateAdminPasswordSecure(passwordForm); setPasswordForm({ currentPassword: '', newPassword: '' }); pushToast('success', 'Password updated successfully.') } catch (error) { pushToast('error', error.message || 'Failed to update password.') } finally { setIsSavingPassword(false) } }
  const filteredProducts = useMemo(() => { const normalizedQuery = query.trim().toLowerCase(); return products.filter((product) => { const target = `${product.name || ''} ${product.category || ''}`.toLowerCase(); return (!normalizedQuery || target.includes(normalizedQuery)) && (productCategoryFilter === 'all' || String(product.category || '').trim() === productCategoryFilter) }) }, [products, query, productCategoryFilter])
  const totalPages = Math.max(1, Math.ceil(filteredProducts.length / pageSize)); useEffect(() => setCurrentPage((page) => Math.min(page, totalPages)), [totalPages]); const pagedProducts = useMemo(() => filteredProducts.slice((currentPage - 1) * pageSize, currentPage * pageSize), [filteredProducts, currentPage])
  const selectedProductsCount = selectedProductIds.length
  const metrics = useMemo(() => { const usersMap = new Map(); orders.forEach((order) => { const key = `${String(order.customerName || '').trim()}-${String(order.customerPhone || '').trim()}`; if (!usersMap.has(key) && (order.customerName || order.customerPhone)) usersMap.set(key, order) }); return { totalProducts: products.length, totalOrders: orders.length, totalUsers: usersMap.size, revenue: orders.reduce((sum, order) => order.paymentStatus === 'paid' ? sum + Number(order.productPrice || 0) : sum, 0) } }, [products, orders])
  const chartData = useMemo(() => { const days = []; const now = new Date(); for (let offset = 6; offset >= 0; offset -= 1) { const date = new Date(now); date.setDate(now.getDate() - offset); days.push({ key: date.toISOString().slice(0, 10), label: date.toLocaleDateString('en-IN', { weekday: 'short' }), total: 0 }) } const map = new Map(days.map((item) => [item.key, item])); orders.forEach((order) => { const timestampMs = getTimestampMs(order.createdAt); const item = map.get(timestampMs ? new Date(timestampMs).toISOString().slice(0, 10) : ''); if (item) item.total += Number(order.productPrice || 0) }); return days }, [orders]); const maxChartValue = Math.max(1, ...chartData.map((item) => item.total)); const recentOrders = useMemo(() => orders.slice(0, 6), [orders])
  const filteredOrders = useMemo(() => { const normalizedQuery = query.trim().toLowerCase(); const result = orders.filter((order) => { const statusMatch = orderStatusFilter === 'all' || String(order.status || '').toLowerCase() === orderStatusFilter; const target = `${order.customerName || ''} ${order.customerPhone || ''} ${order.productName || ''}`.toLowerCase(); return statusMatch && (!normalizedQuery || target.includes(normalizedQuery)) }); return [...result].sort((a, b) => { const left = getTimestampMs(a.createdAt) ?? 0; const right = getTimestampMs(b.createdAt) ?? 0; return orderSort === 'newest' ? right - left : left - right }) }, [orders, orderStatusFilter, orderSort, query])
  const selectedOrder = useMemo(() => filteredOrders.find((order) => order.id === selectedOrderId) || null, [filteredOrders, selectedOrderId]); const filteredOrderIds = useMemo(() => filteredOrders.map((order) => order.id), [filteredOrders]); const allVisibleOrdersSelected = filteredOrderIds.length > 0 && filteredOrderIds.every((id) => selectedOrderIds.includes(id)); useEffect(() => setSelectedOrderIds((current) => current.filter((id) => filteredOrderIds.includes(id))), [filteredOrderIds])
  const handleToggleOrderSelection = (orderId) => setSelectedOrderIds((current) => current.includes(orderId) ? current.filter((id) => id !== orderId) : [...current, orderId])
  const handleSelectAllVisibleOrders = (checked) => setSelectedOrderIds(checked ? filteredOrderIds : [])
  const handleBulkOrderStatusUpdate = async () => { if (!bulkOrderStatus || !selectedOrderIds.length) return; setIsBulkUpdatingOrders(true); try { await Promise.all(selectedOrderIds.map((id) => updateOrderStatus(id, bulkOrderStatus))); setOrders((current) => current.map((order) => selectedOrderIds.includes(order.id) ? { ...order, status: bulkOrderStatus } : order)); setBulkOrderStatus(''); pushToast('success', 'Selected orders updated.') } catch { pushToast('error', 'Failed to update selected orders.') } finally { setIsBulkUpdatingOrders(false) } }
  const handleBulkDeleteOrders = async () => { if (!selectedOrderIds.length) return; setIsBulkDeletingOrders(true); try { await Promise.all(selectedOrderIds.map((id) => deleteOrder(id))); setOrders((current) => current.filter((order) => !selectedOrderIds.includes(order.id))); setSelectedOrderIds([]); pushToast('success', 'Selected orders deleted.') } catch { pushToast('error', 'Failed to delete selected orders.') } finally { setIsBulkDeletingOrders(false) } }
  const handleDeleteUser = async (userId) => { setDeletingUserId(userId); try { await deleteUserProfile(userId); pushToast('success', 'User deleted successfully.') } catch (error) { pushToast('error', error.message || 'Failed to delete user.') } finally { setDeletingUserId('') } }
  const handleNavigation = (key) => { if (key === 'pos') { navigate('/admin/pos'); return } if (key === 'inventory') { navigate('/admin/inventory'); return } setActiveSection(key) }
  return (<>
    <AdminLayout activeKey={activeSection} onChangeKey={handleNavigation} title="Admin Dashboard" query={query} onQueryChange={setQuery} onLogout={handleLogout} isLoggingOut={isLoggingOut}>
      {activeSection === 'dashboard' && <div className="space-y-6"><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><StatCard label="Products" value={metrics.totalProducts} hint="Catalog" /><StatCard label="Orders" value={metrics.totalOrders} hint="Online orders" /><StatCard label="Customers" value={metrics.totalUsers} hint="Unique customers" /><StatCard label="Paid Revenue" value={formatCurrency(metrics.revenue)} hint="Verified paid orders" /></div><div className="grid gap-5 xl:grid-cols-[1.3fr_0.7fr]"><section className="rounded-2xl border border-white/10 bg-[#111111] p-5 shadow-soft"><div className="flex items-center justify-between"><div><h2 className="font-semibold text-white">7-Day Revenue</h2><p className="text-xs text-white/45">Paid online order revenue</p></div></div><div className="mt-6 flex h-56 items-end gap-2">{chartData.map((item) => <div key={item.key} className="flex h-full flex-1 flex-col justify-end gap-2"><div className="rounded-t-lg bg-[#c19a6b]/70" style={{ height: `${Math.max(4, (item.total / maxChartValue) * 100)}%` }} /><p className="text-center text-[10px] text-white/45">{item.label}</p></div>)}</div></section><section className="rounded-2xl border border-white/10 bg-[#111111] p-5 shadow-soft"><h2 className="font-semibold text-white">Recent Orders</h2><div className="mt-4 space-y-3">{recentOrders.length === 0 ? <p className="text-sm text-white/40">No orders yet.</p> : recentOrders.map((order) => <div key={order.id} className="flex items-center justify-between gap-3 rounded-xl border border-white/8 bg-[#0c0c0c] p-3"><div><p className="text-sm font-medium text-white">{order.customerName || 'Customer'}</p><p className="text-xs text-white/45">{order.productName || 'Order'}</p></div><span className="text-sm font-semibold text-[#f0ddc4]">{formatCurrency(order.productPrice)}</span></div>)}</div></section></div></div>}
      {activeSection === 'products' && <div className="space-y-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-[11px] font-semibold uppercase tracking-[0.2em] text-[#c19a6b]">Catalog</p><h1 className="mt-1 text-2xl font-semibold text-white">Products</h1></div><div className="flex gap-2"><Button onClick={handleOpenCreate}>Add Product</Button><Button variant="danger" disabled={selectedProductsCount === 0} onClick={handleBulkDeleteProducts}>Delete Selected ({selectedProductsCount})</Button></div></div><div className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-white/10 bg-[#111111] p-4 shadow-soft"><div className="flex items-center gap-2"><label className="text-xs text-white/50">Category</label><select value={productCategoryFilter} onChange={(event) => setProductCategoryFilter(event.target.value)} className="rounded-xl border border-white/15 bg-[#0f0f0f] px-3 py-2 text-xs text-white/80"><option value="all">All Categories</option>{adminCategories.map((category) => <option key={category} value={category}>{category}</option>)}</select></div><div className="flex gap-1 rounded-xl border border-white/10 bg-[#0f0f0f] p-1"><button type="button" onClick={() => setViewMode('grid')} className={`rounded-xl px-3 py-1.5 text-[11px] font-semibold uppercase tracking-[0.12em] ${viewMode === 'grid' ? 'bg-[#c19a6b] text-black' : 'text-white/65'}`}>Grid</button><button type="button" onClick={() => setViewMode('table')} className={`rounded-xl px-3 py-1.5 text-[11px] font-semibold uppercase tracking-[0.12em] ${viewMode === 'table' ? 'bg-[#c19a6b] text-black' : 'text-white/65'}`}>Table</button></div></div>{isLoadingProducts && <Loader label="Loading products" />}{!isLoadingProducts && filteredProducts.length === 0 && <EmptyState title="No products found" description="Try changing your search query or category filter." />}{!isLoadingProducts && filteredProducts.length > 0 && viewMode === 'grid' && <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{pagedProducts.map((product) => { const productImages = getProductImages(product); const activeImageIndex = activeCardImageIndex[product.id] || 0; const activeImage = productImages[activeImageIndex] || ''; const stock = Number(product.stock || 0); return <article key={product.id} className="rounded-2xl border border-white/10 bg-[#111111] p-3 shadow-soft"><div className="aspect-[4/3] overflow-hidden rounded-xl bg-black/40">{activeImage ? <img src={activeImage} alt={product.name} className="h-full w-full object-cover" /> : <div className="grid h-full place-items-center text-xs text-white/45">No image</div>}</div><div className="mt-3 space-y-2"><div className="flex items-start justify-between gap-2"><h3 className="line-clamp-1 text-sm font-semibold text-white">{product.name}</h3><Badge tone={stock > 0 ? 'success' : 'danger'}>{stock > 0 ? `Stock ${stock}` : 'Out'}</Badge></div><p className="text-xs text-white/60">{product.category || 'Uncategorized'}</p><div className="flex items-center gap-2"><p className="text-sm font-semibold text-[#f0ddc4]">{formatCurrency(product.effectivePrice ?? product.price)}</p>{Number(product.discountPercent || 0) > 0 && <><span className="text-xs text-white/35 line-through">{formatCurrency(product.price)}</span><Badge tone="warning">{Number(product.discountPercent)}% OFF</Badge></>}</div><p className="text-[11px] text-white/40">SKU: {product.sku || '-'} · Barcode: {product.barcode || '-'}</p><div className="grid grid-cols-2 gap-2 pt-1"><Button variant="secondary" onClick={() => handleOpenEdit(product)}>Edit</Button><Button variant="danger" onClick={() => { setDeletingProductId(product.id); setShowDeleteModal(true) }}>Delete</Button><Button variant="ghost" className="col-span-2" onClick={() => handleCopyProductLink(product.id)}>Copy Product Link</Button></div></div></article> })}</div>}{!isLoadingProducts && filteredProducts.length > 0 && viewMode === 'table' && <Table columns={[{ key: 'select', label: '' }, { key: 'product', label: 'Product' }, { key: 'price', label: 'Price' }, { key: 'category', label: 'Category' }, { key: 'stock', label: 'Stock' }, { key: 'sku', label: 'SKU / Barcode' }, { key: 'actions', label: 'Actions' }]}>{pagedProducts.map((product) => <tr key={product.id} className="border-b border-white/8"><td className="px-4 py-3"><input type="checkbox" checked={selectedProductIds.includes(product.id)} onChange={(event) => setSelectedProductIds((current) => event.target.checked ? [...current, product.id] : current.filter((id) => id !== product.id))} className="h-4 w-4" /></td><td className="px-4 py-3 text-sm text-white">{product.name}</td><td className="px-4 py-3 text-sm text-[#f0ddc4]">{formatCurrency(product.effectivePrice ?? product.price)}{Number(product.discountPercent || 0) > 0 && <span className="ml-2 text-xs text-white/40 line-through">{formatCurrency(product.price)}</span>}</td><td className="px-4 py-3 text-sm text-white/70">{product.category || '-'}</td><td className="px-4 py-3"><Badge tone={Number(product.stock || 0) > 0 ? 'success' : 'danger'}>{Number(product.stock || 0)}</Badge></td><td className="px-4 py-3 text-xs text-white/50">{product.sku || '-'}<br />{product.barcode || '-'}</td><td className="px-4 py-3"><div className="flex gap-2"><Button variant="secondary" className="px-3 py-2" onClick={() => handleOpenEdit(product)}>Edit</Button><Button variant="danger" className="px-3 py-2" onClick={() => { setDeletingProductId(product.id); setShowDeleteModal(true) }}>Delete</Button></div></td></tr>)}</Table>}{!isLoadingProducts && filteredProducts.length > pageSize && <div className="flex items-center justify-between rounded-2xl border border-white/10 bg-[#111111] px-4 py-3"><p className="text-xs text-white/60">Page {currentPage} of {totalPages}</p><div className="flex gap-2"><Button variant="secondary" disabled={currentPage === 1} onClick={() => setCurrentPage((page) => page - 1)}>Previous</Button><Button variant="secondary" disabled={currentPage === totalPages} onClick={() => setCurrentPage((page) => page + 1)}>Next</Button></div></div>}</div>}
      {activeSection === 'orders' && <div className="space-y-5"><div className="grid gap-3 rounded-2xl border border-white/10 bg-[#111111] p-4 shadow-soft md:grid-cols-3"><select value={orderStatusFilter} onChange={(event) => setOrderStatusFilter(event.target.value)} className="rounded-2xl border border-white/15 bg-[#0f0f0f] px-3 py-2 text-xs uppercase tracking-[0.12em] text-white/80"><option value="all">All Statuses</option><option value="pending">Pending</option><option value="processing">Shipped</option><option value="completed">Delivered</option><option value="cancelled">Cancelled</option></select><select value={orderSort} onChange={(event) => setOrderSort(event.target.value)} className="rounded-2xl border border-white/15 bg-[#0f0f0f] px-3 py-2 text-xs uppercase tracking-[0.12em] text-white/80"><option value="newest">Newest First</option><option value="oldest">Oldest First</option></select><Button variant="secondary" onClick={loadOrders}>Refresh Orders</Button></div>{filteredOrders.length > 0 && <div className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-white/10 bg-[#111111] p-4 shadow-soft"><div className="flex items-center gap-2"><input type="checkbox" checked={allVisibleOrdersSelected} onChange={(event) => handleSelectAllVisibleOrders(event.target.checked)} className="h-4 w-4" /><p className="text-xs font-semibold uppercase tracking-[0.14em] text-white/60">Selected: {selectedOrderIds.length}</p></div><div className="flex flex-wrap items-center gap-2"><select value={bulkOrderStatus} onChange={(event) => setBulkOrderStatus(event.target.value)} className="rounded-2xl border border-white/15 bg-[#0f0f0f] px-3 py-2 text-xs text-white/80"><option value="">Set status...</option><option value="pending">Pending</option><option value="processing">Shipped</option><option value="completed">Delivered</option><option value="cancelled">Cancelled</option></select><Button variant="secondary" disabled={!bulkOrderStatus || !selectedOrderIds.length || isBulkUpdatingOrders || isBulkDeletingOrders} onClick={handleBulkOrderStatusUpdate}>{isBulkUpdatingOrders ? 'Applying...' : 'Apply Status'}</Button><Button variant="danger" disabled={!selectedOrderIds.length || isBulkUpdatingOrders || isBulkDeletingOrders} onClick={handleBulkDeleteOrders}>{isBulkDeletingOrders ? 'Deleting...' : 'Delete Selected'}</Button><Button variant="ghost" disabled={!selectedOrderIds.length} onClick={() => setSelectedOrderIds([])}>Clear</Button></div></div>}{isLoadingOrders && <Loader label="Loading orders" />}{!isLoadingOrders && filteredOrders.length === 0 && <EmptyState title="No orders found" description="Orders matching this filter will appear here." />}{!isLoadingOrders && filteredOrders.length > 0 && <Table columns={[{ key: 'select', label: '' }, { key: 'id', label: 'Order ID' }, { key: 'customer', label: 'Customer' }, { key: 'products', label: 'Products' }, { key: 'total', label: 'Total' }, { key: 'status', label: 'Status' }, { key: 'date', label: 'Date' }, { key: 'actions', label: 'Actions' }]}>{filteredOrders.map((order) => <tr key={order.id} className="border-b border-white/8"><td className="px-4 py-3"><input type="checkbox" checked={selectedOrderIds.includes(order.id)} onChange={() => handleToggleOrderSelection(order.id)} className="h-4 w-4" /></td><td className="px-4 py-3 text-xs text-white/60">{order.id?.slice(0, 8)}</td><td className="px-4 py-3"><p className="text-sm font-medium text-white">{order.customerName || 'Customer'}</p><p className="text-xs text-white/50">{order.customerPhone || '-'}</p></td><td className="px-4 py-3 text-sm text-white/70">1 item</td><td className="px-4 py-3 text-sm text-[#f0ddc4]">{formatCurrency(order.productPrice)}</td><td className="px-4 py-3"><Badge tone={getOrderStatusTone(order.status)}>{order.status || 'pending'}</Badge></td><td className="px-4 py-3 text-sm text-white/60">{formatDateTime(order.createdAt)}</td><td className="px-4 py-3"><div className="flex gap-2"><Button variant="secondary" className="px-3 py-2" onClick={() => setSelectedOrderId(selectedOrderId === order.id ? null : order.id)}>{selectedOrderId === order.id ? 'Hide' : 'View'}</Button><select value={order.status || 'pending'} onChange={async (event) => { try { await updateOrderStatus(order.id, event.target.value); setOrders((current) => current.map((item) => item.id === order.id ? { ...item, status: event.target.value } : item)); pushToast('success', 'Order status updated.') } catch { pushToast('error', 'Failed to update order status.') } }} className="rounded-xl border border-white/15 bg-[#0f0f0f] px-2 py-1 text-[11px] text-white/75"><option value="pending">Pending</option><option value="processing">Shipped</option><option value="completed">Delivered</option><option value="cancelled">Cancelled</option></select><Button variant="danger" className="px-3 py-2" disabled={deletingOrderId === order.id} onClick={async () => { setDeletingOrderId(order.id); try { await deleteOrder(order.id); setOrders((current) => current.filter((item) => item.id !== order.id)); pushToast('success', 'Order deleted.') } catch { pushToast('error', 'Failed to delete order.') } finally { setDeletingOrderId('') } }}>Delete</Button></div></td></tr>)}</Table>}{selectedOrder && <div className="rounded-2xl border border-white/10 bg-[#111111] p-5 shadow-soft"><h3 className="font-semibold text-white">Order Details</h3><div className="mt-4 grid gap-3 md:grid-cols-2"><p className="text-sm text-white/70">Customer: <span className="text-white">{selectedOrder.customerName || '-'}</span></p><p className="text-sm text-white/70">Phone: <span className="text-white">{selectedOrder.customerPhone || '-'}</span></p><p className="text-sm text-white/70">Product: <span className="text-white">{selectedOrder.productName || '-'}</span></p><p className="text-sm text-white/70">Amount: <span className="text-white">{formatCurrency(selectedOrder.productPrice)}</span></p><p className="text-sm text-white/70 md:col-span-2">Address: <span className="text-white">{selectedOrder.customerAddress || selectedOrder.customerAddressText || '-'}</span></p></div></div>}</div>}
      {activeSection === 'users' && <div className="space-y-5"><h1 className="text-2xl font-semibold text-white">Users</h1><p className="text-sm text-white/50">User management remains available from the existing admin module.</p></div>}
      {activeSection === 'analytics' && <div className="space-y-5"><h1 className="text-2xl font-semibold text-white">Analytics</h1><p className="text-sm text-white/50">Profit analytics will be connected to POS and inventory transactions next.</p></div>}
    </AdminLayout>
    <Modal open={showProductModal} onClose={() => { setShowProductModal(false); resetForm() }} title={editingProduct ? 'Edit Product' : 'Add Product'} maxWidth="max-w-4xl"><form onSubmit={handleSubmitProduct} className="grid gap-4 md:grid-cols-2"><Input label="Name" name="name" value={formValues.name} onChange={handleInputChange} placeholder="Classic Black Shirt" required /><Input label="Sale Price (INR)" name="price" type="number" min="0" step="0.01" value={formValues.price} onChange={handleInputChange} placeholder="1999" required /><Input label="Discount (%)" name="discountPercent" type="number" min="0" max="100" step="0.01" value={formValues.discountPercent} onChange={handleInputChange} placeholder="0" /><Input label="SKU" name="sku" value={formValues.sku} onChange={handleInputChange} placeholder="Auto-generated if blank" /><Input label="Barcode" name="barcode" value={formValues.barcode} onChange={handleInputChange} placeholder="Auto-generated if blank" /><Input label="Purchase Price (INR)" name="purchasePrice" type="number" min="0" step="0.01" value={formValues.purchasePrice} onChange={handleInputChange} placeholder="1200" /><Input label="MRP (INR)" name="mrp" type="number" min="0" step="0.01" value={formValues.mrp} onChange={handleInputChange} placeholder="2499" /><Input label="GST (%)" name="gstPercent" type="number" min="0" max="100" step="0.01" value={formValues.gstPercent} onChange={handleInputChange} placeholder="5" /><Input label="HSN Code" name="hsnCode" value={formValues.hsnCode} onChange={handleInputChange} placeholder="6109" /><Input label="Opening Stock" name="openingStock" type="number" min="0" step="1" value={formValues.openingStock} onChange={handleInputChange} placeholder="20" /><Input label="Opening Stock Value (INR)" name="openingStockValue" type="number" min="0" step="0.01" value={formValues.openingStockValue} onChange={handleInputChange} placeholder="24000" /><label className="space-y-2"><span className="text-[11px] font-semibold uppercase tracking-[0.18em] text-white/70">Category</span><select name="category" value={formValues.category} onChange={handleInputChange} className="w-full rounded-2xl border border-white/15 bg-[#0f0f0f] px-4 py-3 text-sm text-white outline-none focus:border-[#c19a6b]/60">{adminCategories.map((category) => <option key={category} value={category}>{category}</option>)}</select></label><Input className="md:col-span-2" label="Sizes (comma separated)" name="sizesInput" value={formValues.sizesInput} onChange={handleInputChange} placeholder="S,M,L,XL" /><label className="space-y-2 md:col-span-2"><span className="text-[11px] font-semibold uppercase tracking-[0.18em] text-white/70">Description</span><textarea name="description" rows={4} value={formValues.description} onChange={handleInputChange} className="w-full rounded-2xl border border-white/15 bg-[#0f0f0f] px-4 py-3 text-sm text-white outline-none focus:border-[#c19a6b]/60" /></label><label className="space-y-2 md:col-span-2"><span className="text-[11px] font-semibold uppercase tracking-[0.18em] text-white/70">Product Images {editingProduct ? '(Optional for updates)' : ''}</span><input key={fileInputKey} type="file" multiple accept="image/*" onChange={handleImageChange} className="w-full rounded-2xl border border-white/15 bg-[#0f0f0f] px-4 py-2.5 text-sm file:mr-4 file:rounded-xl file:border-0 file:bg-[#c19a6b] file:px-3 file:py-1.5 file:text-xs file:font-semibold file:uppercase file:tracking-[0.12em] file:text-black" />{isSaving && isUploadingImages && <p className="text-xs text-white/60">Uploading images...</p>}{selectedImagePreviews.length > 0 && <div className="grid grid-cols-3 gap-2 sm:grid-cols-4 md:grid-cols-6">{selectedImagePreviews.map((previewUrl, index) => { const imageFile = images[index]; return <div key={previewUrl} className="relative"><img src={previewUrl} alt="Selected product preview" className="aspect-square w-full rounded-xl border border-white/10 object-cover" /><button type="button" onClick={() => handleRemoveSelectedImage(imageFile)} className="absolute right-1 top-1 rounded-full bg-black/80 px-1.5 py-0.5 text-[10px] text-white">x</button></div> })}</div>}{editingProduct && retainedExistingImages.length > 0 && <div className="grid grid-cols-3 gap-2 sm:grid-cols-4 md:grid-cols-6">{retainedExistingImages.map((imageUrl, index) => <div key={`${imageUrl}-${index}`} className="relative"><img src={imageUrl} alt="Current product" className="aspect-square w-full rounded-xl border border-white/10 object-cover" /><button type="button" onClick={() => handleRemoveExistingImage(index)} className="absolute right-1 top-1 rounded-full bg-black/80 px-1.5 py-0.5 text-[10px] text-white">x</button></div>)}</div>}</label><label className="space-y-2 md:col-span-2"><span className="text-[11px] font-semibold uppercase tracking-[0.18em] text-white/70">Size Chart (Text)</span><textarea name="sizeChartText" rows={3} value={formValues.sizeChartText} onChange={handleInputChange} className="w-full rounded-2xl border border-white/15 bg-[#0f0f0f] px-4 py-3 text-sm text-white outline-none focus:border-[#c19a6b]/60" /></label><label className="space-y-2 md:col-span-2"><span className="text-[11px] font-semibold uppercase tracking-[0.18em] text-white/70">Size Chart Image</span><input key={`${fileInputKey}-size-chart`} type="file" accept="image/*" onChange={handleSizeChartImageChange} className="w-full rounded-2xl border border-white/15 bg-[#0f0f0f] px-4 py-2.5 text-sm file:mr-4 file:rounded-xl file:border-0 file:bg-[#c19a6b] file:px-3 py-1.5 text-xs font-semibold uppercase tracking-[0.12em] file:text-black" /></label><div className="md:col-span-2 flex flex-wrap gap-2"><Button type="submit" variant="primary" disabled={isSaving}>{isSaving ? 'Saving...' : editingProduct ? 'Update Product' : 'Add Product'}</Button><Button type="button" variant="secondary" onClick={() => { setShowProductModal(false); resetForm() }}>Cancel</Button></div></form></Modal>
    <Modal open={showDeleteModal} onClose={() => { setShowDeleteModal(false); setDeletingProductId('') }} title="Confirm Delete" maxWidth="max-w-lg" footer={<div className="flex gap-2"><Button variant="danger" onClick={() => handleDelete(deletingProductId)} disabled={!deletingProductId}>Delete</Button><Button variant="secondary" onClick={() => { setShowDeleteModal(false); setDeletingProductId('') }}>Cancel</Button></div>}><p className="text-sm text-white/70">This action cannot be undone. Delete selected product?</p></Modal>
    <AnimatePresence>{toast.message && <Motion.div initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 12 }} className={`fixed bottom-5 right-5 z-[160] rounded-2xl border px-4 py-3 text-sm shadow-elevated ${toast.type === 'error' ? 'border-red-500/35 bg-red-500/15 text-red-100' : 'border-[#c19a6b]/40 bg-[#c19a6b]/15 text-[#f0ddc4]'}`}>{toast.message}</Motion.div>}</AnimatePresence>
  </> )
}

export default Admin
